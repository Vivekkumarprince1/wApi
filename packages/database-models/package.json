{
  "name": "@wapi/database-models",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Mongoose schemas for wApi services and apps. Version-neutral: schemas are bound to a caller-provided connection so each consumer uses its own mongoose.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "mongoose": ">=8"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "mongoose": "^8.0.0",
    "typescript": "^6.0.3"
  }
}
