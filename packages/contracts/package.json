{
  "name": "@wapi/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared request/response types and event payloads for wApi services.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node dist-test/__tests__/roles.test.js && node dist-test/__tests__/redis-config.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^6.0.3"
  }
}